# Personal site

Dead-simple but beautiful multi-page site in the spirit of deadsimplesites.com. Plain HTML, CSS, and JS. No build step. Open `index.html` in a browser to
preview, or serve it with `python3 -m http.server`.
